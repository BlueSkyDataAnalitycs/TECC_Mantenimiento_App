# Archivo de datos generado — se reconstruye con `python3 build.py`
datos.json

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Excel originales con datos personales (NO publicar en repo público)
data_vehiculos/

# Secretos / llaves (NUNCA se suben). La app NO usa llaves; esto es solo para build.py opcional.
.env

# Datos personales de empleados (NO publicar)
brayan_tegw.json
*_empleados*.json
TECC_Empleados_backup.json
CLAUDE.md
